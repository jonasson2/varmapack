Installation
============

Install Varmapack with ``pip``:

.. code-block:: sh

   pip install varmapack

For ordinary use, importing ``varmapack`` is sufficient. Users who want explicit
control over seeding or parallel streams can additionally create and pass a
``randompack.Rng`` object.
