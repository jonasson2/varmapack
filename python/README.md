# varmapack

Python interface to Varmapack, a VARMA time-series simulation library.

This package builds the C extension from the C sources synced into `python/src`.
Run `../scripts/syncpy.sh` from the repository root before building a release
package.
