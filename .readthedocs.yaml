version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - gfortran
    - pkg-config
  jobs:
    install:
      - bash python/docs/rtd-install.sh

sphinx:
  configuration: python/docs/conf.py
