version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - gfortran
    - libopenblas-dev
    - pkg-config

sphinx:
  configuration: python/docs/conf.py

python:
  install:
    - requirements: python/docs/requirements.txt
    - method: pip
      path: python
