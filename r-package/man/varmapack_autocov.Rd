% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{varmapack_autocov}
\alias{varmapack_autocov}
\title{Sample autocovariances of an observed series}
\usage{
varmapack_autocov(X, maxlag, norm = "ML")
}
\arguments{
\item{X}{Numeric \code{n} by \code{r} observed time-series matrix.}

\item{maxlag}{Largest lag to compute, between zero and \code{n - 1}.}

\item{norm}{Either \code{"ML"}, which divides every lag by \code{n}, or \code{"C"}, which
divides the lag-\code{k} covariance by \code{n - k}.}
}
\value{
An \code{r} by \code{r} by \code{maxlag + 1} array. Its \code{k + 1} plane estimates
\verb{Cov(x_t, x_\{t-k\})}.
}
\description{
Compute sample autocovariance matrices up to a specified lag for a numeric
time-series matrix with observations in rows and variables in columns.
}
\examples{
X <- cbind(1:10, (1:10)^2)
varmapack_autocov(X, maxlag = 2)

}
