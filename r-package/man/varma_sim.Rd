% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varma_sim.R
\name{varma_sim}
\alias{varma_sim}
\title{Simulate VARMA(p, q) Time Series}
\usage{
varma_sim(A, B, Sig, n, mu = NULL, M = 1, X0 = NULL,
          park_miller = FALSE, seed = 42)
}
\arguments{
\item{A}{An \eqn{r \times r \times p} array of AR coefficient matrices.
When \eqn{p = 1}, an \eqn{r \times r} matrix is also accepted.}

\item{B}{An \eqn{r \times r \times q} array of MA coefficient matrices.
When \eqn{q = 1}, an \eqn{r \times r} matrix is also accepted.}

\item{Sig}{An \eqn{r \times r} covariance matrix of the shocks
(must be positive-definite).}

\item{n}{Positive integer, length of generated series.}

\item{mu}{Optional mean vector of length \eqn{r} (default \code{NULL} for
zero-mean).}

\item{M}{Positive integer number of replicates (default 1).}

\item{X0}{Optional \eqn{r \times \max(p,q)} matrix of initial values. If it
specified, each generated series starts with X0, otherwise the series 
start with random entries (default \code{NULL} for "not specified").}

\item{park_miller}{Logical; use Park-Miller RNG (default \code{FALSE}).}

\item{seed}{Integer seed for Park-Miller RNG (default 42).}
}
\value{
A list with components:
\describe{
  \item{X}{An \eqn{r \times n \times M} array of simulated series.}
  \item{eps}{An \eqn{r \times n \times M} array of shock terms.}
  \item{ok}{Status flag: \code{1} if simulation succeeded (model
    is stationary or \code{X0} is not \code{NULL})', \code{0} otherwise.}
}
}
\description{
Generate spin-up free multivariate time series from a VARMA(p, q) model:
\deqn{x_t = \mu + \sum_{i=1}^p A_i x_{t-i} +
         \sum_{j=1}^q B_j \varepsilon_{t-j} + \varepsilon_t,}
where \eqn{\varepsilon_t} are zero-mean shocks with covariance \eqn{\Sigma}.
}
\examples{
A   <- array(c(0.5, -0.2, 0.1, 0.3), dim = c(2,2))
B   <- array(0, dim = c(2,2))
Sig <- diag(2)
out <- varma_sim(A, B, Sig, n = 30)
str(out)
out$X
# Plot the simulated series for the first replicate
matplot(t(out$X[,,1]), type = "l", lty = 1,
        main = "Simulated VARMA Series", xlab = "Time", ylab = "Value")
legend("topright", legend = paste("Series", 1:nrow(out$X)),
       col = seq_len(nrow(out$X)), lty = 1)

}
