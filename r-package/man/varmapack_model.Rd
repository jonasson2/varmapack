% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{varmapack_model}
\alias{varmapack_model}
\title{Create a VARMA or VARMAX model}
\usage{
varmapack_model(A = NULL, B = NULL, C = NULL, Sig, mu = NULL)
}
\arguments{
\item{A}{Autoregressive coefficient matrices as an \code{r} by \code{r} matrix or an
\code{r} by \code{r} by \code{p} array. \code{NULL} gives no autoregressive terms.}

\item{B}{Moving-average coefficient matrices as an \code{r} by \code{r} matrix or an
\code{r} by \code{r} by \code{q} array. \code{NULL} gives no moving-average terms.}

\item{C}{Exogenous coefficient matrices as an \code{r} by \code{d} matrix or an
\code{r} by \code{d} by \code{s} array. \code{NULL} gives a VARMA model rather than VARMAX.}

\item{Sig}{\code{r} by \code{r} innovation covariance matrix.}

\item{mu}{Optional \code{r} vector or \code{r} by \code{nmu} matrix of time-series means.
The last mean vector repeats for the rest of each series. Means are not
supported for VARMAX models.}
}
\value{
A \code{VarmapackModel} object.
}
\description{
Create a model object for simulation of a Gaussian VAR, VMA, VARMA, or
VARMAX time series. The object stores model parameters and provides a
\verb{$sim()} method for generating one or more independent series.
}
\section{Simulation}{

\code{model$sim(length, nrep = 1L, X0 = NULL, z = NULL, rng = NULL, return_shocks = FALSE)} simulates \code{nrep} independent series of the supplied
\code{length}.

For VARMA models, \code{X0} is optional. If provided, it is an \code{r} by \code{nX0}
matrix or an \code{r} by \code{nX0} by \code{nrep} array. Its second dimension must be at
least \code{max(p, q)}, and its third dimension, when present, must be 1 or
\code{nrep}.
Nonstationary VARMA models require \code{X0}; with MA terms, \code{Sig} must be
positive definite and startup shocks are conditioned on the residual
equations implied by the supplied history.

For VARMAX models, \code{z} is required. \code{X0} has the same form and its second
dimension must be at least \code{max(p, q, s - 1)}. It may be omitted when this
minimum is zero. The exogenous input \code{z} is a \code{d} by \code{length} matrix or a \code{d}
by \code{length} by \code{nrep} array. Its third dimension, when present, must be 1 or
\code{nrep}.

Pass a \code{randompack::randompack_rng()} object through \code{rng} to control the
random stream. When it is omitted, Varmapack uses a temporary randomized
default Randompack generator. With \code{return_shocks = FALSE}, the result is an
\code{r} by \code{length} by \code{nrep} array. With \code{return_shocks = TRUE}, it is a list
with components \code{X} and \code{E}, each with that shape.
}

\section{Model analysis}{

\code{model$acvf(maxlag)} returns theoretical VARMA autocovariances,
\code{model$psi(maxlag)} returns impulse-response matrices, and
\code{model$irf(maxlag)} returns orthogonalized impulse-response matrices.
\code{model$specrad()} and \code{model$ma_specrad()} return the AR and MA spectral
radii, respectively. Theoretical autocovariances are not available for
VARMAX models with exogenous terms.
}

\examples{
A <- matrix(c(0.4, 0.1, -0.2, 0.3), 2, 2)
model <- varmapack_model(A = A, Sig = diag(2))
X <- model$sim(100)

rng <- randompack::randompack_rng()
rng$seed(123)
X <- model$sim(100, nrep = 3, rng = rng)

}
